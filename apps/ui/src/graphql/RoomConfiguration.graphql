query RoomingConfiguration($tourCode: String!) {
    roomingConfiguration(tourCode: $tourCode) {
        _id
        bedCode
        name
        roomInventory {
            availability
        }
    }
}
